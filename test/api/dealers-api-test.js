import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { playtimeService } from "./playtime-service.js";
import { maggie, mozart, testCountys, testDealers, concerto } from "../fixtures.js";

suite("Dealer API tests", () => {
  let user = null;
  let beethovenSonatas = null;

  setup(async () => {
  });

  teardown(async () => {});

  test("create dealer", async () => {
  });

  test("create Multiple dealerApi", async () => {
  });

  test("Delete Dealer", async () => {
  });

  test("test denormalised county", async () => {
  });
});
