{{> menu active="dashboard"}}

<section class="section">
  <div class="title">
    {{county.title}}

  </div>
  {{> list-dealers showBrandOption=showBrandOption}}
  {{#if showBrandOption}}
    {{> add-dealer}}
  {{/if}}
  
</section>