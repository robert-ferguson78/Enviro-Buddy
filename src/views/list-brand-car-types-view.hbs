{{> menu active="dashboard"}}

<section class="section">
  {{> list-car-types}}
    {{> add-car-type}}
  {{#each messages}}
    <div class="alert alert-info">
      {{this}}
    </div>
  {{/each}}
</section>